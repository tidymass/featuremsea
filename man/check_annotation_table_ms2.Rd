% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions_data_checking.R
\name{check_annotation_table_ms2}
\alias{check_annotation_table_ms2}
\title{Check the Format of an MS2 Annotation Table}
\usage{
check_annotation_table_ms2(check_annotation_table_ms2)
}
\arguments{
\item{check_annotation_table_ms2}{A `data.frame` containing MS2 annotation results.
It must include the following columns: `variable_id`, `mz`, `rt`, `HMDB.ID`, and `KEGG.ID`.}
}
\value{
Returns `TRUE` (invisibly) if all checks pass. Otherwise, stops with an informative error message.
}
\description{
Validates that the MS2 annotation table meets required structural and type specifications.
The function checks for required columns and ensures proper data types for each.
}
\details{
The function performs the following checks:
\itemize{
  \item `check_annotation_table_ms2` must be a data frame
  \item Required columns: `variable_id`, `mz`, `rt`, `HMDB.ID`, `KEGG.ID`
  \item `variable_id`: character, unique
  \item `mz` and `rt`: numeric
  \item `HMDB.ID` and `KEGG.ID`: character (can include `NA`)
}
}
\examples{
# Example annotation table
annotation_table <- data.frame(
  variable_id = c("M1", "M2"),
  mz = c(100.1, 200.2),
  rt = c(300, 400),
  HMDB.ID = c("HMDB00001", NA),
  KEGG.ID = c("C00031", NA),
  stringsAsFactors = FALSE
)

check_annotation_table_ms2(annotation_table)

}
\author{
Xiaotao Shen \email{xiaotao.shen@outlook.com}
}
