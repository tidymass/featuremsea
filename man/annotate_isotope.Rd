% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotate_isotope.R
\name{annotate_isotope}
\alias{annotate_isotope}
\title{Annotate Isotopic Peaks for a Given Compound Formula}
\usage{
annotate_isotope(
  formula = "C9H14N2O12P2",
  adduct = "M-H",
  charge = 1,
  mz = 402.9998,
  rt = 823.1462,
  mean_intensity = 1000,
  peak.mz,
  peak.rt,
  peak.int,
  rt.tol = 5,
  mz.tol = 15,
  int.tol = 0.3,
  max.isotope = 3
)
}
\arguments{
\item{formula}{Character. The molecular formula (e.g., `"C9H14N2O12P2"`).}

\item{adduct}{Character. The adduct form (e.g., `"M-H"`, `"M+H"`).}

\item{charge}{Integer. The ion charge. Default is `1`.}

\item{mz}{Numeric. The observed m/z of the monoisotopic peak `[M]`.}

\item{rt}{Numeric. The retention time (in seconds) of the monoisotopic peak.}

\item{mean_intensity}{Numeric. The mean intensity of the monoisotopic peak.}

\item{peak.mz}{Numeric vector. Observed m/z values of all detected peaks.}

\item{peak.rt}{Numeric vector. Observed retention times of all detected peaks.}

\item{peak.int}{Numeric vector. Observed intensities of all detected peaks.}

\item{rt.tol}{Numeric. RT tolerance (in seconds) for matching isotopic peaks. Default is `5`.}

\item{mz.tol}{Numeric. m/z tolerance (in ppm) for matching. Default is `15`.}

\item{int.tol}{Numeric. Intensity ratio tolerance. Default is `0.3`.}

\item{max.isotope}{Integer. Maximum number of isotopes to predict (e.g., 3 includes `[M+1]`, `[M+2]`, `[M+3]`). Default is `3`.}
}
\value{
A data frame of matched isotopic peaks with the following columns:
\describe{
  \item{`peakIndex`}{Index of matched peak in the original peak list}
  \item{`mzError.ppm`}{m/z error in ppm}
  \item{`isotopes`}{Isotope label (e.g., `[M+1]`, `[M+2]`)}
  \item{`rtError.s`}{RT error in seconds}
}
Returns `NULL` if no `[M+1]` isotopic peak is matched.
}
\description{
Predicts theoretical isotopic peaks for a given compound formula and adduct, then matches them to observed peaks based on m/z,
retention time (RT), and intensity within user-defined tolerances. Supports up to `[M+n]` isotope matching.
}
\details{
The function uses the `Rdisop` package to predict isotope distribution, then filters observed peaks
by RT, m/z, and intensity match to detect isotopic features. It prioritizes matches with minimal intensity error.
}
\examples{
\dontrun{
annotate_isotope(
  formula = "C9H14N2O12P2",
  adduct = "M-H",
  mz = 402.9998,
  rt = 823.1462,
  mean_intensity = 1000,
  peak.mz = c(403.0, 404.003, 405.008),
  peak.rt = c(823.2, 823.3, 823.5),
  peak.int = c(1000, 500, 200),
  rt.tol = 5,
  mz.tol = 15,
  int.tol = 0.3,
  max.isotope = 2
)
}

}
\author{
Xiaotao Shen \email{xiaotao.shen@outlook.com}
}
