% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotate_feature_table.R
\name{score_annotation_table}
\alias{score_annotation_table}
\title{Score Metabolite Feature Clusters (MFCs) Based on RT Grouping}
\usage{
score_annotation_table(annotation_table, mfc_rt_tol = 10)
}
\arguments{
\item{annotation_table}{A data frame containing annotated metabolite features. Must include at least the columns
`Lab.ID` and `rt` (retention time).}

\item{mfc_rt_tol}{Numeric. Retention time tolerance (in seconds) used for grouping features into the same cluster. Default is 10.}
}
\value{
A data frame with an additional `metabolite_feature_cluster` column (unique cluster ID) and `score` column for each feature.
}
\description{
Assigns metabolite feature clusters (MFCs) within each compound (`Lab.ID`) by grouping features based on retention time (RT)
and calculating a cluster-level score using `score_mfc()`. Each feature is tagged with a `metabolite_feature_cluster` identifier and a computed `score`.
}
\details{
\itemize{
  \item Features are grouped by `Lab.ID`, then clustered into MFCs based on RT proximity.
  \item Each MFC is scored via the `score_mfc()` function (assumed to be user-defined).
  \item Parallel computation is used via `furrr::future_map()` for faster processing.
}
}
\examples{
\dontrun{
data("example_annotation_table")
scored_table <- score_annotation_table(annotation_table = example_annotation_table, mfc_rt_tol = 5)
head(scored_table)
}

}
\author{
Xiaotao Shen \email{xiaotao.shen@outlook.com}
}
